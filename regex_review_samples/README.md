# Regex Review Samples

Put real `.doc` or `.docx` samples in `input/`.

Run:

```powershell
.\.venv\Scripts\python.exe .\review_regex_samples.py
```

Reports are written to `reports/` as JSON and CSV. Real input files and generated reports are ignored by git.
